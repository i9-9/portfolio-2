"use client";

import { LinesLoaderScreen } from "@/components/splash/LinesLoader";

/** Next.js route fallback — same moving lines as the landing splash. */
export default function Loading() {
  return <LinesLoaderScreen />;
}
